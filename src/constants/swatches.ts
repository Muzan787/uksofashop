// src/constants/swatches.ts

/**
 * Three samples, free, anywhere on the UK mainland.
 *
 * It lives here rather than in the dialog that first needed it because two
 * separate surfaces now offer the service — the fabric picker on a
 * made-to-order product, and the guide at /fabrics — and a limit that says
 * three in one place and four in the other is the kind of thing nobody
 * notices until a customer counts.
 */
export const MAX_SAMPLES = 3;
